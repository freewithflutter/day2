// first we will initialize the data , we wiil used that in provider

class NormalPlace {
  String image, title, place, price, placeType, aboutPlace;
  int rateCount, openHour, aim;
  double rate;

  NormalPlace(
      {this.image,
      this.title,
      this.place,
      this.price,
      this.placeType,
      this.aboutPlace,
      this.rate,
      this.aim,
      this.rateCount,
      this.openHour});
}
